// extern crate rand;
// extern crate piston_window;

mod snake;

use opengl_graphics::{GlGraphics, OpenGL};

struct Window {
    gl: GlGraphics, 
}

impl Window {
    // fn render()
}

fn main() {
    let (width, height) = (40, 4);

}
